services:
  web:
    build:
      context: .
    image: perma-filecheck
    volumes:
      - .:/app
    ports:
      - "127.0.0.1:8080:8080"
    environment:
      - DOCKERIZED=True
    extra_hosts:
      - "filecheck.perma.test:127.0.0.1"
    cap_add:
      - SYS_PTRACE
